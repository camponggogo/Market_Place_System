# คู่มือการใช้งานระบบ (User Manual)

**Market_Place_System – ระบบจัดการตลาดนัด Onlinehelp**

เอกสารนี้สำหรับผู้ใช้งานระบบ ได้แก่ ผู้ดูแลระบบ (Admin), พนักงานร้านค้า (Store POS), พนักงาน Counter, ลูกค้า และผู้ที่ใช้งานจอ Signage

---

## สารบัญ

1. [ภาพรวมระบบ](#1-ภาพรวมระบบ)
2. [Admin Dashboard](#2-admin-dashboard)
3. [Store POS (หน้าจอ POS ร้านค้า)](#3-store-pos-หน้าจอ-pos-ร้านค้า)
4. [Launch – เปิด Store POS + Signage (2 จอ)](#4-launch--เปิด-store-pos--signage-2-จอ)
5. [Signage (จอที่ 2 แสดง QR / ข้อความได้รับเงิน)](#5-signage-จอที่-2-แสดง-qr--ข้อความได้รับเงิน)
6. [Counter (จุดแลกเงิน / คืนเงิน / Top-up)](#6-counter-จุดแลกเงิน--คืนเงิน--top-up)
7. [Customer (ลูกค้า – ตรวจสอบยอด / ขอคืนเงิน)](#7-customer-ลูกค้า--ตรวจสอบยอด--ขอคืนเงิน)
8. [ตัวอย่างการใช้งาน (Scenarios)](#8-ตัวอย่างการใช้งาน-scenarios)
9. [คำถามที่พบบ่อย (FAQ)](#9-คำถามที่พบบ่อย-faq)

---

## 1. ภาพรวมระบบ

ระบบแบ่งการใช้งานหลักเป็น 4 กลุ่ม:

| กลุ่มผู้ใช้ | หน้าที่หลัก | URL หลัก |
|-------------|-------------|----------|
| **Admin** | ดูสถิติ, รายการธุรกรรม, จัดการลูกค้า/ร้าน | `/admin` |
| **ร้านค้า (Store POS)** | ใส่เงินที่หัก, สร้าง QR ชำระเงิน, หักยอดด้วย Food Court ID | `/store-pos?store_id=1` |
| **Counter** | แลกเงินเป็น Food Court ID, คืนเงิน, Top-up | ใช้ API / ระบบ Counter |
| **ลูกค้า** | ตรวจสอบยอดคงเหลือ, ขอคืนเงิน | `/customer`, `/customer-qr` |

**จอ Signage (จอที่ 2):** แสดง QR Code และข้อความ "ได้รับเงินเรียบร้อยแล้ว" ให้ลูกค้ามองจากจอใหญ่

---

## 2. Admin Dashboard

**URL:** `/admin`

### การใช้งาน

- **สถิติภาพรวม:** ดูสถิติการใช้งานระบบ
- **รายการธุรกรรม:** ดูรายการหักยอด / แลกเงิน / คืนเงิน
- **รายการลูกค้า:** ดูและจัดการลูกค้า
- **ผูก Food Court ID กับลูกค้า:** ใช้เมนู/ฟอร์ม Link Food Court ID
- **ยกเลิกการผูก Food Court ID:** ลบการผูกเมื่อไม่ใช้แล้ว

### หมายเหตุ

- หน้า Admin อาจเชื่อมกับ API `/api/admin/statistics`, `/api/admin/transactions`, `/api/admin/customers` ตามที่พัฒนาหน้าเว็บไว้

---

## 3. Store POS (หน้าจอ POS ร้านค้า)

**URL:** `/store-pos?store_id=1` (เปลี่ยน `store_id` ตามร้าน)

ใช้สำหรับพนักงานร้านค้า: ใส่จำนวนเงินที่หัก, สร้าง QR ชำระเงิน (PromptPay), หักยอดด้วย Food Court ID

### ขั้นตอนที่ 1: ใส่จำนวนเงินที่ต้องการหัก

1. กรอก **จำนวนเงิน (บาท)** ในช่องใหญ่ หรือกดปุ่ม **ราคาด่วน** (50, 100, 200, 500 บาท ฯลฯ)
2. กดปุ่ม **✓ ยืนยัน** ที่ Numeric Keypad เพื่อยืนยันจำนวนเงิน
3. (ถ้าต้องการแก้) กดที่ช่องจำนวนเงินอีกครั้งเพื่อเปิด Keypad แล้วแก้ไข

### ขั้นตอนที่ 2: ข้อมูลสำหรับ PromptPay QR Code

1. **เลือกรายการสินค้า (Menu):** เลือกเมนูจากรายการ (ถ้ามี) ระบบจะเติมราคารวมให้อัตโนมัติ
2. **กรอกราคารวม (Total Price):** จำนวนเงินที่ลูกค้าต้องจ่าย (บาท)
3. **หมายเหตุ (Remark):** กรอกได้ตามต้องการ (เช่น เลขโต๊ะ, ชื่อลูกค้า)
4. **PromptPay Mobile (Tag29):** ถ้าต้องการ QR แบบโอนเข้าบัญชีบุคคล (Credit Transfer) ให้กรอกเบอร์ 10 หลัก
5. กดปุ่ม **📱 สร้าง PromptPay QR Code**

หลังสร้าง QR จะเห็น:
- **Tag30 – Bill Payment:** QR สำหรับธุรกิจ (Static + Dynamic ตามยอด)
- **Tag29 – Credit Transfer:** QR สำหรับบุคคล (ถ้ากรอกเบอร์แล้ว)
- ปุ่ม **ยกเลิก – ทำรายการใหม่:** กดเพื่อยกเลิกการจ่ายและทำรายการใหม่ (จอ Signage จะกลับโหมดปกติ)

### ขั้นตอนที่ 3: สแกนหรือกรอก Food Court ID

1. กด **📷 เปิดกล้องสแกน QR Code** เพื่อสแกน QR บัตร/ใบเสร็จของลูกค้า  
   หรือ **กรอก Food Court ID** ลงในช่อง (เช่น `FC-20241201-00001`)
2. กดปุ่ม **💳 หักยอดเงิน**
3. ระบบจะตรวจสอบยอดคงเหลือและหักเงินให้
4. ถ้ายอดไม่พอ ระบบจะแจ้งและแสดงยอดคงเหลือ
5. **🔄 ล้างข้อมูล:** กดเมื่อต้องการล้างฟอร์มทั้งหมดและเริ่มรายการใหม่

### ปุ่มราคาด่วน (Quick Amount)

- **คลิกสั้น:** ใช้ราคานั้นเป็นจำนวนเงินที่หัก (ขั้นตอนที่ 1)
- **กดค้าง (Long press):** เปิดหน้าต่างแก้ไขราคาด่วน (แก้แล้วบันทึก)

### หมายเหตุ

- หลังสร้าง QR ระบบจะส่งภาพ QR ไปจอ **Signage** (จอที่ 2) อัตโนมัติ
- เมื่อมีเงินเข้า (Webhook จากธนาคาร) ระบบจะแจ้ง "จ่ายเงินเรียบร้อยแล้ว" และพูด TTS ให้

---

## 4. Launch – เปิด Store POS + Signage (2 จอ)

**URL:** `/launch?store_id=1`

ใช้เมื่อมี **2 จอ:** จอ 1 = Store POS, จอ 2 = Signage

### การใช้งาน

1. เปิดเบราว์เซอร์แล้วเข้า `/launch?store_id=1`
2. ระบบจะเปิด 2 หน้าต่าง:
   - หน้าต่างที่ 1: **Store POS** (สำหรับพนักงาน)
   - หน้าต่างที่ 2: **Signage** (จอใหญ่ให้ลูกค้ามอง)
3. วางจอที่ 1 ไว้ที่เคาน์เตอร์ร้าน จอที่ 2 ไว้ด้านบนหรือด้านข้างให้ลูกค้าเห็น
4. หน้าต่าง Launch จะปิดตัวเองหลังจากเปิด 2 หน้าต่างแล้ว

### หมายเหตุ

- ถ้าเครื่องมีหลายจอ ระบบจะพยายามเปิด Store POS บนจอหลัก และ Signage บนจอที่สอง (ขึ้นกับเบราว์เซอร์และสิทธิ์)

---

## 5. Signage (จอที่ 2 แสดง QR / ข้อความได้รับเงิน)

**URL:** `/signage` (หรือเปิดผ่าน Launch)

### การทำงาน

- **โหมดปกติ:** แสดงข้อความต้อนรับ / รอการชำระเงิน
- **โหมดรอชำระ:** เมื่อ Store POS สร้าง QR แล้ว จอ Signage จะแสดง **QR Code** และ **ยอดเงิน** ให้ลูกค้าสแกน
- **โหมดได้รับเงินแล้ว:** เมื่อระบบรับการแจ้งเงินเข้า (Webhook) จะแสดงข้อความ **"ได้รับเงินเรียบร้อยแล้ว"** และพูด TTS (เช่น "เงินเข้าแล้ว X บาท ขอบคุณครับ") จากนั้นกลับไปโหมดปกติ

### สำหรับผู้ดูแล

- จอ Signage จะ poll ข้อมูลจาก API `/api/signage/display?store_id=...` เป็นระยะ
- การยกเลิกที่ Store POS (ปุ่ม "ยกเลิก – ทำรายการใหม่") จะล้างจอ Signage กลับโหมดปกติ

---

## 6. Counter (จุดแลกเงิน / คืนเงิน / Top-up)

การทำงานที่ Counter ใช้ผ่าน **API** หรือระบบ Counter ที่พัฒนาต่อจาก API

### 6.1 การแลกเงินเป็น Food Court ID

1. ลูกค้าแจ้งจำนวนเงินและวิธีชำระเงิน (เงินสด / PromptPay / ฯลฯ)
2. พนักงาน Counter เรียก API **POST `/api/counter/exchange`** โดยส่ง:
   - `amount`: จำนวนเงิน
   - `payment_method`: วิธีชำระ (เช่น `cash`)
3. ระบบออก **Food Court ID** (เช่น `FC-20241201-00001`) และบันทึกยอดเริ่มต้น
4. มอบ Food Court ID (หรือ QR Code ที่สร้างจาก ID นี้) ให้ลูกค้า

### 6.2 การตรวจสอบยอดคงเหลือ

- เรียก **GET `/api/counter/balance/{foodcourt_id}`** เพื่อดูยอดคงเหลือของ Food Court ID นั้น

### 6.3 การคืนเงิน

1. ลูกค้านำ Food Court ID (หรือ QR) มาแจ้งที่ Counter
2. พนักงานเรียก **GET `/api/counter/balance/{foodcourt_id}`** เพื่อตรวจยอดคงเหลือ
3. เรียก **POST `/api/counter/refund`** ส่ง `foodcourt_id`
4. ระบบหักยอดคงเหลือเป็น 0 และบันทึกการคืนเงิน
5. คืนเงินให้ลูกค้าตามวิธีที่กำหนด

### 6.4 การ Top-up (เติมเงิน)

1. ลูกค้าแจ้ง Food Court ID และจำนวนเงินที่ต้องการเติม
2. พนักงานเรียก **POST `/api/counter/topup`** ส่ง `foodcourt_id`, `amount`, `payment_method`
3. ระบบเพิ่มยอดคงเหลือให้ Food Court ID นั้น

---

## 7. Customer (ลูกค้า – ตรวจสอบยอด / ขอคืนเงิน)

**URL หลัก:** `/customer`, `/customer-qr`

### 7.1 ตรวจสอบยอดคงเหลือ

- เปิดหน้า **Customer** หรือ **Customer QR**
- กรอก **Food Court ID** หรือสแกน QR ที่บัตร/ใบเสร็จ
- ระบบแสดงยอดคงเหลือและประวัติ (ตามที่พัฒนาหน้าเว็บไว้)

### 7.2 ขอคืนเงิน

- ลูกค้าไปที่ **Counter** พร้อม Food Court ID (หรือ QR)
- พนักงาน Counter ทำการคืนเงินตามขั้นตอนในหัวข้อ Counter ข้างต้น

---

## 8. ตัวอย่างการใช้งาน (Scenarios)

### Scenario 1: ลูกค้าใหม่

1. ลูกค้าไปที่ Counter พร้อมเงินสด 500 บาท
2. พนักงาน Counter แลก Food Court ID ให้ ได้ `FC-20241201-00001`
3. ลูกค้าไปสั่งอาหารที่ร้าน A 150 บาท → พนักงานร้านกด Store POS: ใส่ 150, สร้าง QR (ถ้าจ่าย PromptPay) หรือสแกน Food Court ID แล้วกดหักยอด
4. ยอดคงเหลือ 350 บาท
5. ลูกค้าไปสั่งเครื่องดื่มที่ร้าน B 50 บาท → หักยอดเหมือนเดิม เหลือ 300 บาท
6. ลูกค้าไปที่ Counter ขอคืนเงิน 300 บาท → พนักงาน Counter ทำ Refund

### Scenario 2: ร้านใช้ PromptPay + จอ Signage

1. ลูกค้าสั่งอาหาร 120 บาท
2. พนักงาน Store POS: ใส่จำนวนเงินที่หัก 120, กรอกราคารวม 120, กดสร้าง QR
3. จอ Signage แสดง QR และยอด 120 บาท ลูกค้าสแกนจ่ายผ่านแอปธนาคาร
4. เมื่อเงินเข้า ระบบแจ้ง "จ่ายเงินเรียบร้อยแล้ว" และจอ Signage แสดง "ได้รับเงินเรียบร้อยแล้ว" + พูด TTS
5. พนักงานกดยกเลิกหรือทำรายการถัดไป

---

## 9. คำถามที่พบบ่อย (FAQ)

**Q: Food Court ID หมดอายุหรือไม่?**  
A: ไม่หมดอายุตามระบบ แต่อาจมีนโยบายให้ใช้ภายในวันหรือภายในงานตามที่จัดตั้งไว้

**Q: สามารถโอน Food Court ID ให้คนอื่นได้หรือไม่?**  
A: ได้จากมุมมองระบบ แต่แนะนำให้ใช้เองเพื่อความปลอดภัย

**Q: ถ้า Food Court ID หายทำอย่างไร?**  
A: ติดต่อ Counter พร้อมหลักฐานการแลก (ถ้ามี) เพื่อพิจารณาคืนเงิน

**Q: จอ Signage ไม่แสดง QR?**  
A: ตรวจว่า Store POS สร้าง QR แล้ว และจอ Signage เปิดที่ `/signage` (และ poll กับ store_id ที่ถูกต้อง)

**Q: ปุ่ม "ยกเลิก – ทำรายการใหม่" ใช้เมื่อไร?**  
A: ใช้เมื่อต้องการยกเลิกรายการจ่ายปัจจุบัน (ไม่หักยอด) และเริ่มรายการใหม่ จอ Signage จะกลับโหมดปกติ

**Q: เปิด API Docs (Swagger/ReDoc) อย่างไร?**  
A: ตั้งค่า environment `ENABLE_DOCS=true` แล้วรัน Server จากนั้นเปิด `/docs` หรือ `/redoc`

---

## Support

หากมีปัญหาหรือคำถามเกี่ยวกับการใช้งาน:
- ดูเอกสารเพิ่ม: [README](../README.md), [API Documentation](API_DOCUMENTATION.md)
- ติดต่อผู้ดูแลระบบหรือทีมพัฒนา Market_Place_System
